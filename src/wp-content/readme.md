## `src/wp-content`

wp-content
├── plugins
│   └── your-plugin
│       ├── your-plugin.php
│       └── other-plugin-files
├── themes
└── uploads
└── ...

can build any wordpress `plugins` in the plugin directory
can build any wordpress `themes` in the theme directory
